{
  "name": "duplo-build",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "node --experimental-strip-types src/bricks/stack.check.ts && node --experimental-strip-types src/frame.check.ts && node --experimental-strip-types src/status.check.ts"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "@types/three": "^0.186.0"
  },
  "dependencies": {
    "three": "^0.181.2"
  }
}
